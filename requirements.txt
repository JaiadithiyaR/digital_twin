# Core numerical / data
numpy>=2.0
pandas>=2.2
scipy>=1.13
pyarrow>=17.0

# Classical ML (DT prediction components)
scikit-learn>=1.5
xgboost>=2.1

# RL (PPO decision agent)
torch>=2.4
gymnasium>=0.29
stable-baselines3>=2.3
matplotlib>=3.8  # PPO training reward-curve reporting only, not used in any deterministic module

# LLM (regeneration / expand-scope / verification / lifecycle reasoning)
anthropic>=0.34

# RAG knowledge base
chromadb>=0.5

# Telemetry transport (Python side; bundles its own libzmq, no system lib required)
pyzmq>=26.0

# Config / validation / secrets
pyyaml>=6.0
pydantic>=2.8
pydantic-settings>=2.4
python-dotenv>=1.0

# Testing
pytest>=8.3
pytest-cov>=5.0

# Networkx used for dependency-graph topological ordering in the DT orchestrator
networkx>=3.3
